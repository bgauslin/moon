donut-chart {
  height: 100%;
  position: absolute;
  width: 100%;

  text {
    font-size: em(13);
  }

  // Blend moon arc with sun arc where they overlap.
  &[name='moon'] circle {
    opacity: .5;
  }
}
