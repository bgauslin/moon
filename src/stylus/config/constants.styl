@import 'functions'

// Values are coordinated with 'Chart' enum in 'source/js/modules/Constants.js'
// @type Hash
Chart = {
  SIZE: 320
  SWEEP_WIDTH: 72
  MARGIN: 44
};

// @type String
AXIS_LABEL_SIZE = px_to_rem(32)

// @type String
BUTTON_SIZE = px_to_rem(32)

// @type String
CHART_MAX_WIDTH = px_to_rem(480)

// @type Number
CHART_SIZE_UNITS = Chart.SIZE + (Chart.MARGIN * 2)

// @type String
CHART_SIZE = px_to_rem(CHART_SIZE_UNITS)

// @type String
CONTROLS_SIZE = px_to_rem(48)

// @type String
LOCATION_HEIGHT = px_to_rem(32)

// @type String
LOCATION_MAX_WIDTH = px_to_rem(Chart.SIZE)

// @type Number
PHOTO_SIZE = Chart.SIZE - (Chart.SWEEP_WIDTH * 2)

// @type String
PHOTO_WIDTH = (PHOTO_SIZE / CHART_SIZE_UNITS) * 100%

// @type Hash
Breakpoint = {
  SMALL: 'screen and (min-width: %s)' % (CHART_SIZE + (AXIS_LABEL_SIZE + CONTROLS_SIZE * 2))
}
