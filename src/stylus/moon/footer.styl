.footer
  align-items center
  align-self center
  color rgba(white, .5)
  display flex
  font-size var(--fineprint)
  grid-row 3
  height var(--footer-height)
  justify-content center
  padding 0 0 env(safe-area-inset-bottom)

.footer__years
  margin-right em(8)

.footer__link
  link(rgba(white, .5), rgba(white, .5), white, white)

.footer__link:focus
[no-touch] .footer__link:hover
  transition color TRANSITION

.footer__blurb--repo::before
  content '·'
  display inline-block
  text-align center
  width 1rem
